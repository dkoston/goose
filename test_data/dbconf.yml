test:
    driver: postgres
    open: user=postgres dbname=test sslmode=disable host=localhost port=5432

test2:
    driver: postgres
    open: user=postgres dbname=test2 sslmode=disable host=localhost port=5432

# docker run -d -p 5432:5432 postgres:9.5
# psql -U postgres -p 5432 -h localhost
# CREATE DATABASE test;
# CREATE DATABASE test2;
# goose -path=test_data -env=test up chat_client_config_service
# psql -U postgres -p 5432 -h localhost test
# SELECT * FROM chat_client_config_service_db_version;
# goose -path=test_data -env=test2 up
# psql -U postgres -p 5432 -h localhost test2
# SELECT * FROM goose_db_version;